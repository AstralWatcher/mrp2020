from setuptools import setup

setup(name='serbianer',
      version='0.1',
      description='Named entity recognition for Serbian langugage',
      packages=['serbianer'],
      zip_safe=False, install_requires=['pandas'])
